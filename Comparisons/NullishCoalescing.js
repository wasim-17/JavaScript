const name = `Wasim Akram`;
const provider = `Hotstar`;
const phoneNumber = `98556 61177`;
const subscriptionType = null;
// const subscriptionType = `Premium`;

console.log(`
    Creating ${subscriptionType ?? 'Guest'} Profile for ${name} with ${provider} at ${phoneNumber}`
);